### 并查集

`非递归写法`

```c++
int find_set(int x){
    int r = x;
    while(fa[r] != r)	r = fa[r];
    int i = x, j;
    while(i != r){
        j = fa[i];
        fa[i] = r;
        i = j;
    }
    return r;
}
```

